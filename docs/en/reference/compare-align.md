# Reference: compare & align

## Compare

- `DefaultComparer.compare(leftSnapshotId, rightSnapshotId, opts) -> DiffResult`
- `DefaultComparer.compareSubtree(leftSnapshotId, leftBase, rightSnapshotId, rightBase, opts) -> DiffResult`

See also:

- `docs/en/explanations/compare-and-moves.md`

## Align

- `DefaultAligner.align(snapshotIds, scope, strategy, mode) -> AlignmentResult`

See also:

- `docs/en/explanations/alignment-row-keys.md`

